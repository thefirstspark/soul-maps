# Soul Maps — site

soul-maps.thefirstspark.shop. GitHub Pages from `master`.

This repo holds only what the browser needs: rendered Soul Map pages, `archive.html`/`index.html`, `card.html` (reads `signatures.json`), the Color Codex pages, `codex-colors.css`, `codex-wheel.js`, sitemap/robots.

The engine that generates these pages (generator, webhook server, monthly regenerator, tests) lives in the PRIVATE repo `thefirstspark/soul-map-engine` as of 2026-09-05. Do not add engine code here.

Rules: `SITE_CONSTITUTION.md`. Canon: `Desktop/Sparkverse files/SPARK-CANON-v1.3.md`.
