# CLAUDE.md

**Read `SITE_CONSTITUTION.md` in this repo first.** It is the shared rulebook for every First Spark web property (canonical URLs, live Whop plan IDs, design language, retire-don't-delete, no page without a hub link + Notion inventory row). This file only adds repo-specific notes.

## Repo notes
- Customer maps are `[initials][mmddyyyy].html`, generated by `soul_map_generator.py`; monthly updates are `-YYYYMM.html`. Never hand-edit a generated map's numbers.
- `signatures.json` is the permanent Quantum Signature registry (first mint wins). Never regenerate or reorder it.
- Color Codex source of truth is each map's badge; `codex-colors.css` holds the palette.
- `card.html` (?p1/?p2) is what Frequency Match links to.
